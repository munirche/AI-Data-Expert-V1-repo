{
  "use_case_name": "Patient Registration",
  "output_directory": "./data_entry_output",

  "whisper_model": "base",
  "whisper_language": "en",

  "extraction_fields": {
    "patient_first_name": {
      "type": "string",
      "required": true,
      "description": "Patient first name"
    },
    "patient_middle_initial": {
      "type": "string",
      "required": false,
      "description": "Patient middle initial (single letter)"
    },
    "patient_last_name": {
      "type": "string",
      "required": true,
      "description": "Patient last name"
    },
    "date_of_birth": {
      "type": "string",
      "required": true,
      "description": "Patient date of birth (MM/DD/YYYY format)"
    },
    "sex_at_birth": {
      "type": "enum",
      "required": true,
      "description": "Sex at birth",
      "values": ["male", "female"]
    },
    "address": {
      "type": "string",
      "required": true,
      "description": "Patient full address (street, city, state, zip)"
    },
    "phone_number": {
      "type": "string",
      "required": true,
      "description": "Patient phone number"
    },
    "emergency_contact_name": {
      "type": "string",
      "required": true,
      "description": "Emergency contact full name"
    },
    "emergency_contact_phone": {
      "type": "string",
      "required": true,
      "description": "Emergency contact phone number"
    },
    "emergency_contact_relation": {
      "type": "string",
      "required": true,
      "description": "Emergency contact relationship to patient (e.g., spouse, parent, sibling)"
    }
  },

  "extraction_prompt_template": "You are a data extraction assistant. Extract structured fields from the following transcript.\n\nTranscript:\n{transcript}\n\nExtract the following fields into a JSON object:\n{schema}\n\nRules:\n- Only extract information that is explicitly stated or clearly implied in the transcript.\n- If a field's value is not mentioned, set it to null.\n- For array fields, return an empty array [] if nothing is mentioned.\n- For enum fields, pick the closest matching value from the allowed options.\n- Return ONLY valid JSON, no markdown, no extra text."
}
